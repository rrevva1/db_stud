## TOAST и хранение больших значений

**Часть:** Внутреннее устройство PostgreSQL · **Модуль:** Хранение и расширения

Фокус на теории и тесте; SQL не обязателен.

### Цели урока

- Объяснить механизм TOAST
- Выбрать стратегию storage для JSON/BLOB
- Диагностировать раздувание строк

### Краткая теория

**TOAST** — хранение значений > ~2 КБ вне основной страницы.

| STORAGE | Поведение |
|---------|-----------|
| EXTENDED | Сжатие + out-of-line (default text/json) |
| EXTERNAL | Out-of-line без сжатия |
| MAIN | Сжатие, out-of-line если не влезает |
| PLAIN | Без TOAST |

Большие JSON хранятся в **TOAST-таблице**; heap tuple — указатель. `pg_column_size(row)` — полный размер.

Рекомендации: не тащить BLOB в hot OLTP-row; GIN по нужным ключам JSON; редко обновляемые большие поля — в отдельную 1:1 таблицу.

### Что читать в источниках

1. **1-я очередь.** Моргунов Е. П. — PostgreSQL. Основы языка SQL — Глава 19, §19.2
2. **2-я очередь.** Новиков Б. А. и др. — Основы технологий баз данных — Глава 13, §13.3
### Ключевые понятия

- **TOAST** — oversized attribute storage.
- **Out-of-line** — данные вне heap page.
- **pg_column_size** — размер строки с TOAST.
